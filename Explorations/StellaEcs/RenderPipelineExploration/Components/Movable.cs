using System;

namespace StellaInvicta.Components;

/// <summary>
/// Identity tag to mark that an entity can be moved by user commands.
/// Only entities with this component will respond to right-click move orders.
/// </summary>
public struct Movable { }
