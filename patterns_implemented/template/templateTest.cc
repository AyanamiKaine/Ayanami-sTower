#include "template.h"
#include <gtest/gtest.h>

// The Test file is irrelevant. 

TEST(TemplateTest, TemplateExample) {
    //CSVProcessor csvProcessor;
    //csvProcessor.processFile("data.csv");
    
    //XMLProcessor xmlProcessor;
    //xmlProcessor.processFile("data.xml");
}